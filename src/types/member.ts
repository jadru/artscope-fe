export type ArtistFormType = {
  introduction: string;
  history: string;
  snsUrl: string;
  websiteUrl?: string;
};

export type CuratorFormType = {
  introduction: string;
  history: string;
  snsUrl: string;
  companyName?: string;
  companyRole?: string;
  websiteUrl?: string;
};
